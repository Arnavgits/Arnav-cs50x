#include <stdio.h>
#include <cs50.h>

void row(int m);
int main(void)

{
    do
    {
        int n= get_int("height:");
    }
    while(n < 8 && n > 0);

    for(i=0 ; i<n ; i++)
    {
        row()
    }
}

void row(int m, int o)
{
    for(i=0 ; i < m ; i++)

}

