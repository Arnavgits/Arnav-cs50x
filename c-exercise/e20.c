#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int a = get_int("Initial no: ");
    int b = get_int("Ending no: ");

    for(int i = a; i <= b; i++)
    {
        if(i % 2 == 0)
        {
            
        }
    }

}
