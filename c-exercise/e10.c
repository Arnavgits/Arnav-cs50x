#include <cs50.h>
#include <stdio.h>

int main(void)
{
    do
    {
    int p = get_int("p: ");
    }
    while (p % 2 = 0)

    unsigned q = get_int("q: ");
    unsigned r = get_int("r: ");
    unsigned s = get_int("s: ");
}
