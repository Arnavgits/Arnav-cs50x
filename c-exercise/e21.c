#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int a = ("No of integers to input: ");
    int b[a];
    b[] = get_int("Enter no:");

    for(int i = 0; i <= a; i++);
    {
        if(a[i] < a[i + 1])
        {
            int c = a[i]
        }
    }

}
