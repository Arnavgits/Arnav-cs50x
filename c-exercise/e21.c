#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int c = 0;
    int b[a];

    int a = ("No of integers to input: ");

    b[] = get_int("Enter no:");

    for(int i = 0;i < a; i++)
    {
        if(a[i] = b[c])
        {
            c = c + 1;
        }
    }

    for(int i = 0; i <= a; i++);
    {
        if(a[i] < a[i + 1])
        {
            int c = a[i]
        }
    }

}
