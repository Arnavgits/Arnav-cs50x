#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int d = 0:
    int b[a];

    int a = ("No of integers to input: ");

    b[] = get_int("Enter no:");

    for(int i = 0;i < a; i++)
    {
    for(int c = 0; c <= a; c++)
    {
        if(b[i] = b[c] && i != c)
        {
            d = d + 1;
        }
    }
    }

    for(int i = 0; i <= a; i++);
    {
        if(a[i] < a[i + 1])
        {
            int e = a[i]
        }
    }

}
