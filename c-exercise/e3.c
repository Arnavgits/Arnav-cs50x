#include <cs50.h>
#include <stdio.h>

int main(void)
{
    long id[10] = get_long("Input the Employees ID(Max. 10 chars): ");
    int hours = get_int("Input the working hrs: ");
    int salary = get_int("Salary amount/hr: ")
}
