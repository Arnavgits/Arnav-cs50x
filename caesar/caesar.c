#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    int cipher = 0;

    string plain = get_string("Enter text: ");
    int a = strlen(plain);

    for(int i = 0;i < a; i++)
    {
        cipher = ()
    }
}
