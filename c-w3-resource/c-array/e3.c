#include <cs50.h>
#include <stdio.h>

int mai(void)
{
    int a = get_int("array elements to enter: ");
    int b[a];

    for(int i = 0; i < a; i++)
    {
        b[i] = get_int("element %i: ")
    }
    
}
