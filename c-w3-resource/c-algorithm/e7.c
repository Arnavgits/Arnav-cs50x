#include <cs50.h>
#include <stdio.h>

int main(void)
{
    do
    {
        int a = get_int("a: ");
        int b = get_int("b: ");
    }
    while(a < 0 && b < 0)

    int c = ((a >= 20 && a <= 30) || (b >= 20 && b <= 30)) ?
}
