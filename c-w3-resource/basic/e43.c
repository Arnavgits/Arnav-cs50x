#include <stdio.h>

int main(void)
{
    int a,b;

    printf("Input no of lines:")
    scanf("%i", a);

    printf("Input no of characters:")
    scanf("%i", b);

    for (int i = 0; i <= a; i++)
    {
        
    }
}
