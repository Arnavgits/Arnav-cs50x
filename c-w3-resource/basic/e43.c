#include <stdio.h>

int main(void)
{
    int a,b;

    printf("Input no of lines:");
    scanf("%i", &a);

    printf("Input no of characters:");
    scanf("%i", &b);

    for (int i = 1; i <= a; i++)
    {
        for (int j = 1; j <= 6 * i; j++)
        {
            printf("%i",  );
        }
        printf("\n");
    }
}
