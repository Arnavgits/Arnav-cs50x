#include <stdio.h>

int main(void)
{
    int a;
    printf("No of elements to enter: ");
    scanf("%i", a);

    number[a];

    int *b = malloc(sizeof(int) * a);

    for (int i = 0; i < a; i++)
    {
        printf("Number %i:", i + 1);
        scanf("%i", number[i]);
    }


}
