#include <stdio.h>

int main(void)
{
    int a;
    printf("No of element to store: ");
    scanf("%i", a);

    int b[a];

    for (int i = 0; i < a; i++)
    {
        printf(""element - %i )
    }
}
