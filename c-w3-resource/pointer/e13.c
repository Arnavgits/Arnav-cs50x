#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int i = 0;
    char *a = malloc(sizeof(char) * 100);
    printf("Input a string: ");
    scanf("%s", a);

    while(a[i] != '\0')
    {
        if(a[i] = 'a' || )
    }
}
