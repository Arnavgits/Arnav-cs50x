#include <stdio.h>

int gcd(int x, int y);

int main(void)
{
    int a,b;
    printf("Enter 1st no:");
    scanf("%i", &a);
    printf("2nd no:");
    scanf("%i", &b);

    int result = gcd(a, b);
}

int gcd(int x, int y)
{
    
}
