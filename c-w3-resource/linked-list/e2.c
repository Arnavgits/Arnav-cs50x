#include <stdio.h>

typedef struct nod
{
    int number;
    nod *next;
}node;

int main(void)
{
    int a;
    printf("Input the number of nodes: ")
    scanf("%i", a);

    for (int i = 0; i < a; i++)
    {
        
    }
}
