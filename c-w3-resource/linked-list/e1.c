#include <stdio.h>

typedef struct nod
{
    int a;
    struct nod *next;
}node;

int main(void)
{
    node *head = malloc(sizeof(node));
    node *b = a;

    b -> a = 1;
    b -> next = malloc(sizeof(node));

    b = b->next;
    b->head = 2;
    b->next = malloc(sizeof(node));

    b = b->next;
    b->head = 3;
    n->next = NULL;

    
}
