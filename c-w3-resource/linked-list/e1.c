#include <stdio.h>

typedef struct nod
{
    int a;
    struct nod *next;
}node;

int main(void)
{
    node *head = malloc(sizeof(node));
    node *b = a;

    head -> a = 1;
    head -> next = malloc(sizeof(node));
}
