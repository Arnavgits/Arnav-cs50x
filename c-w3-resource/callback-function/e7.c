#include <stdio.h>
#include <string.h>

int main(void)
{
    char str[100];

    printf("Enter a string: ");
    scanf("%s", str);
    int size = strlen(str);

    printf("Original string: %s\n", str);

    for (int i = 0; i < size; i++)
    {
        
    }
}
