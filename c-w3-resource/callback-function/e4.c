#include <stdio.h>
#include <ctype.h>

int main(void)
{
    char * str[100];
    printf("String: ");
    scanf("%s", &str);

    int size = strlen(str);

    for (int i = 0; i < size; i++)
    {
        if (str(i) == str(size - i - 1))
        {
            
        }
    }
}
