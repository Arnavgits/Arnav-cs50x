#include <cs50.h>
#include <stdio.h>

int main(int n)
{
}

int collatz(int n)
{
    for (int i = 0; i > 0; i++)
    if( n == 1)
    {
        return 1;
    }
    else if(n == 2 * i)
    {
        do
        {
            n = n / 2
        }
        while()
    }
}
