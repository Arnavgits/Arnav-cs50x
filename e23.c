#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int a = get_int("No of values to enter: ");

    int b[a];

    for(i = 0; i <= a; i++)
    {
        a[i] = get_int("element - %i : ");
    }
}
